<h1><%=@book.title%></h1>
<div>
Author: <%=@book.author %></h3><br>
Genre: <%=@book.genre.name%></h4><br>
</div
<div>
    <p>Synopsis:<br>
    <%=@book.synopsis%></p>
</div>


<%=form_for @book do |f| %>
    <%= f.collection_select :lists, current_user.lists, :id, :title, {:prompt => 'Add this Book to a List' } %> 
    <%= f.submit "Add to List" %>
<%end%><br>


<%= link_to "Write a Review", new_book_review_path(@book) %> 
<h3>Reviews</h3> 
<%= @book.reviews.count %> reviews<br>

<% @reviews.each do |review| %> 
    <strong><%= review.user.username %></strong> rated it <%= stars(review.stars) %> stars - <%=review_date(review.created_at)%><br>
    <%= review.content %><br> 
<%= link_to "see more", review_path(review) %><br><br>
<%end%>


