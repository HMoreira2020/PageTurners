<h2><%=link_to @review.user.username, user_path(@review.user) %>'s Reviews > <%= link_to @review.book.title, book_path(@review.book) %> </h2>

<strong><%= @review.user.username %></strong> rated it <%= @review.stars %> stars - <%=review_date(@review.created_at)%><br>
    <%= @review.content %><br> 

<% if @review.user == current_user %> 
    <%= button_to "Edit Review", edit_review_path(@review) %>
<%end%><br>
<% if @review.user == current_user %> 
        <%= button_to "Delete Review", review_path(@review), method: :delete, data: { confirm: "Are you sure?"} %>
<%end%><br>
  