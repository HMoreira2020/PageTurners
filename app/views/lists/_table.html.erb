<tbody>
<tr>
  <th scope="row"></th>
  <td><%= link_to truncate(book.title), book_path(book) %></%></td>
  <td><%= book.author %></td>
  <td><%= link_to book.genre.name, genre_path(book.genre) %></td>
  <td><%= truncate(book.synopsis, length: 300) %></td>
  <td><%= book.average_rating %></td>
  <td><%= stars(book.average_rating) %></td>
  <td><%= link_to "Write a Review", new_book_review_path(book), class: "btn btn-primary"  %></td>
  <%if policy(list).update?%>
  <td><%= link_to 'Remove', list_book_path(list.id, book.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  <%end%>
  
</tr>
</tbody>